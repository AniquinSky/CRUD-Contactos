
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Buscar Contacto</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-5">
    <h2 class="mb-4">Buscar Contacto por ID</h2>
    <form action="/buscar" method="POST" class="mb-4">
      <input type="number" name="id" required class="form-control w-25 d-inline-block" placeholder="ID">
      <button class="btn btn-primary ms-2">Buscar</button>
    </form>

    <% if (contacto) { %>
      <div class="card p-3">
        <h5><strong>ID:</strong> <%= contacto.id %></h5>
        <p><strong>Nombre:</strong> <%= contacto.nombre %></p>
        <p><strong>Domicilio:</strong> <%= contacto.domicilio %></p>
        <p><strong>Correo:</strong> <%= contacto.correo %></p>
        <p><strong>Teléfono:</strong> <%= contacto.telefono %></p>
      </div>
    <% } else if (typeof contacto !== 'undefined') { %>
      <div class="alert alert-warning">No se encontró ningún contacto con ese ID.</div>
    <% } %>

    <a href="/" class="btn btn-secondary mt-4">Volver al inicio</a>
  </div>
</body>
</html>
