<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= contacto.id ? "Editar" : "Agregar" %> Contacto</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card-form {
      border-radius: 15px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
      border: none;
    }
    .form-header {
      background-color: #4e73df;
      color: white;
      border-radius: 15px 15px 0 0 !important;
    }
    .btn-submit {
      background-color: #4e73df;
      border: none;
      padding: 10px 25px;
      font-weight: 600;
      transition: all 0.3s;
    }
    .btn-submit:hover {
      background-color: #3a5bbf;
      transform: translateY(-2px);
    }
    .form-control {
      border-radius: 8px;
      padding: 12px 15px;
      border: 1px solid #e3e6f0;
    }
    .form-control:focus {
      border-color: #4e73df;
      box-shadow: 0 0 0 0.2rem rgba(78, 115, 223, 0.25);
    }
    label {
      font-weight: 600;
      color: #5a5c69;
      margin-bottom: 8px;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="card card-form">
          <div class="card-header py-3 form-header">
            <h3 class="m-0"><i class="fas fa-address-book me-2"></i><%= contacto.id ? "Editar" : "Agregar" %> Contacto</h3>
          </div>
          <div class="card-body p-5">
            <form action="<%= action %>" method="POST">
              <div class="mb-4">
                <label for="nombre" class="form-label">Nombre:</label>
                <input type="text" name="nombre" id="nombre" value="<%= contacto.nombre || '' %>" 
                       class="form-control" required placeholder="Ingrese el nombre completo">
              </div>
              
              <div class="mb-4">
                <label for="domicilio" class="form-label">Domicilio:</label>
                <input type="text" name="domicilio" id="domicilio" value="<%= contacto.domicilio || '' %>" 
                       class="form-control" required placeholder="Ingrese la dirección">
              </div>
              
              <div class="mb-4">
                <label for="correo" class="form-label">Correo electrónico:</label>
                <input type="email" name="correo" id="correo" value="<%= contacto.correo || '' %>" 
                       class="form-control" required placeholder="ejemplo@dominio.com">
              </div>
              
              <div class="mb-4">
                <label for="telefono" class="form-label">Teléfono:</label>
                <input type="text" name="telefono" id="telefono" value="<%= contacto.telefono || '' %>" 
                       class="form-control" required placeholder="Ingrese el número telefónico">
              </div>
              
              <div class="d-flex justify-content-between mt-5">
                <a href="/" class="btn btn-outline-secondary px-4">
                  <i class="fas fa-times me-2"></i>Cancelar
                </a>
                <button class="btn btn-submit text-white px-4" type="submit">
                  <i class="fas fa-save me-2"></i>Guardar
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Font Awesome para iconos -->
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
